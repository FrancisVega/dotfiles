# ~/.bashrc - configuración interactiva mínima y modular

# Salir si no es interactivo
[[ $- != *i* ]] && return

# Prompt simple (ajústalo si usas starship)
export PS1="\u@\h \w $ "

# Historial básico
HISTCONTROL=ignoreboth
HISTSIZE=10000
HISTFILESIZE=20000
shopt -s histappend
shopt -s checkwinsize
